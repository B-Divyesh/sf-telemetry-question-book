{
  "name": "telemetry-question-book",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests api scripts/*.mjs playwright.config.ts eslint.config.js",
    "build": "npm run typecheck && npm --prefix api ci --omit=dev --silent && vite build && node scripts/write-build-info.mjs",
    "preview": "vite preview",
    "deploy": "bash scripts/deploy.sh",
    "verify:live-api": "node scripts/verify-live-api.mjs",
    "test:api": "node --test api/tests/*.test.js",
    "test": "npm run build && npm run test:api && playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.58.2",
    "eslint": "10.0.1",
    "typescript": "5.9.2",
    "typescript-eslint": "8.68.0",
    "vite": "7.3.6"
  }
}
